name: Merge to Main Workflow

on:
  workflow_run:
    workflows: ["Laravel Sail CI"]
    types:
      - completed
  pull_request:
    types:
      - closed

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: echo "TEST!"
